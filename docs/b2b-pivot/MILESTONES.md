# MILESTONES

버전(마일스톤)별로 뭐가 들어가는지 기록한다. `ROADMAP_V2.md`의 **Phase**가 "개발 순서"라면,
여기 **버전**은 "`main`에 실제로 릴리스된 단위"다 — 여러 Phase가 한 버전으로 묶여 병합되기도
하고, 한 Phase가 여러 버전에 걸쳐 나뉘어 들어가기도 한다.

버전 번호는 시맨틱 버저닝(`MAJOR.MINOR.PATCH`)을 따르고 `package.json`의 `version`과 항상
일치시킨다. `main`에 의미 있는 PR이 머지될 때마다 이 표와 `package.json`을 함께 갱신한다.

| 버전 | 상태 | 포함 범위 |
|------|------|-----------|
| v0.1.0 | 릴리스됨 (main, 2026-07-07) | B2B 피벗 이전 베이스라인 — PERSONAL 방 중심 B2C 데모 |
| v0.2.0 | 릴리스됨 (main, PR #2, 2026-08-10) | Phase 1.5~8.5: Venue/Room 확장(고정 QR, 위치 반경, 입장 비밀번호) · Seating(좌석 배치+운영자 좌석이동) · Guest Care(경고 메시지) · 요일별 영업시간+자동 마감 · Staff(교대/친절도 투표) · Operator 정식 인증(Supabase Auth) · Operator 대시보드 · B2B 전용 전환(PERSONAL 기능 삭제) |
| v0.3.0 | 릴리스됨 (main, PR #2, 2026-08-10) | Phase 9: 홈 지도/Discovery 리디자인(검색·지역 TOP10·매장 상세 바텀시트) + 방 화면 리디자인(4탭 재구성·외부 QnA·좌석배치 에디터·매장 소개) |
| v0.3.1 | 계획 (ADR-0001, ADR-0002) | 좌석 선택 전용 화면 + 방 입장 게이팅, 지도 매장 화면 미리보기(관찰자 뷰) — 문서만 확정, 코드 반영 예정 |
| v0.4.0 | 미착수 (Phase 9.5) | Subscription 도메인(구독/결제), 지도 구독 배지, Reaction/Chat/Seating 쓰기 게이트 |
| v0.5.0 | 미착수 (Phase 10) | 운영자 대시보드 화면 개선 + 필터 UI |

## 원칙

- 버전 경계는 "사용자가 실제로 써볼 수 있는 단위"를 기준으로 잡는다 — Phase 하나가 끝났다고
  자동으로 버전이 올라가지는 않는다(작은 Phase 여러 개가 한 버전으로 묶일 수 있음)
- `main`에 아직 머지되지 않은 버전은 "코드 완료, main 미반영" 또는 "미착수"로 상태를 명확히
  구분한다 — "완료"는 오직 `main`에 실제로 들어간 것만
- 이 표는 `ROADMAP_V2.md`(Phase 상세 스코프)를 대체하지 않는다 — 이 표는 "언제 뭐가 나갔는지"
  요약이고, 각 항목의 세부 규칙/미확정 사항은 `ROADMAP_V2.md`/`BUSINESS_RULES.md`를 따라간다
