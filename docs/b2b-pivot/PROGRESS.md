# PROGRESS

날짜별 작업 로그 + TODO 겸용. 각 항목에 상태 태그를 붙인다 — ✅ 완료 / 🔲 진행중(할 일).
완료된 항목은 지우지 않고 그대로 둔다(그날 뭘 했는지 나중에 찾을 수 있어야 함).

정합성 점검 크론(일일/주간, `AGENTS.md` 참고)이 발견한 문서-코드 어긋남도 여기 새 항목으로
추가된다 — 크론은 이 파일에 기록만 하고, `BUSINESS_RULES.md`/`ROADMAP_V2.md`는 직접 고치지
않는다.

---

## 2026-08-10

- ✅ 문서 정합성 감사 — `BUSINESS_RULES.md`/`ROADMAP_V2.md`/두 README를 실제 코드·마이그레이션과
  대조. 외부 QnA(`venue_qna`), 좌석배치 에디터(`venue_layout_items`), `venues.description`,
  방 화면 리디자인의 로드맵 스코프 누락, 좌석 선택 게이트 문구가 실제 코드와 어긋난 것 등
  5개 항목 발견
- ✅ ADR-0001: 좌석 선택 = 방 입장 전제조건으로 재확정 (`BUSINESS_RULES.md` §2.8 갱신)
- ✅ ADR-0002: 지도 매장 화면 미리보기(관찰자 뷰) 신설 (`BUSINESS_RULES.md` §2.7 갱신)
- ✅ External QnA 도메인(§2.15), 좌석배치 에디터 규칙(§2.8), 매장 소개(§2.2) 문서 백필
- ✅ `ROADMAP_V2.md`에 "Phase 9 상세 — 방 화면 리디자인" 섹션 신설(사후 기록)
- ✅ ADR-0003: 문서 체계(`AGENTS.md`/`CLAUDE.md`/`decisions/`/`PROGRESS.md`/`VARIABLES.md`) 도입,
  정합성 크론 2단계(일일 기계적 체크 / 주간 의미적 감사) 설계
- 🔲 ADR-0001 코드 반영 — 전용 좌석 선택 화면 신설 + 방 입장 게이팅 (지금 코드는 좌석 미선택
  상태로도 방 화면 4탭에 바로 진입하는 예전 버전). **v0.1.0 필수 스코프**
- 🔲 ADR-0002 코드 반영 — 지도 매장 화면 미리보기(관찰자 뷰) 컴포넌트. v0.1.0 필수 스코프에는
  안 들어감(ROADMAP_V2.md "포함" 목록 참고) — 나중으로 미뤄도 됨
- ✅ `docs/b2b-pivot/README.md` 삭제 (`AGENTS.md`의 문서 지도와 중복)
- ✅ 일일/주간 정합성 크론 등록 — 일일 매일 00:00 UTC(≈09:00 KST), 주간 매주 월요일 00:00 UTC. 둘 다 발견만 하고 PROGRESS.md에 기록, 권위 문서는 직접 수정하지 않음
- ✅ 브랜치 → 프리뷰 → 프로덕션 워크플로우 확정 (`AGENTS.md` "워크플로우" 섹션) — 문서만 고칠 땐
  브랜치 불필요, 코드는 매번 새 브랜치+Vercel 프리뷰+PR, `main` 머지는 항상 사용자가 직접
- ✅ `docs/b2b-pivot/MILESTONES.md` 신설 — 버전(v0.1.0~v0.5.0)별 스코프. Phase(개발 순서)와는
  별개 축으로, "main에 실제로 릴리스된 단위" 기준
- ✅ 체크포인트 PR #2 생성 (`claude/mystar-b2b-pivot-cynp6t` → `main`) — 지금까지 61커밋(Phase
  1.5~9 + 오늘 문서 작업) 전체. 머지는 사용자가 직접 검토 후 진행, Claude는 머지하지 않음
- ✅ PR #2 사용자가 검토 후 머지 완료 (`main` ea3e134)
- ✅ 브랜치 삭제 안 함으로 결정 (사용자) — 정합성 크론 2개가 `claude/mystar-b2b-pivot-cynp6t`를
  이름으로 참조하고 있어서, 지웠으면 다음 크론 실행이 실패했을 것. 브랜치는 유지, 대신 PR #2가
  머지되면 크론 프롬프트를 `main` 기준으로 갱신할 것 (아래 항목)
- ✅ 일일/주간 정합성 크론 프롬프트를 `claude/mystar-b2b-pivot-cynp6t` 대신 `main` 기준으로 갱신
- ⚠️ **되돌림**: `package.json` version을 0.3.0으로, `MILESTONES.md`의 버전 경계·포함 범위를
  Claude가 사용자 확인 없이 임의로 정해서 채웠던 걸 사용자가 지적 — 사용자 지시 없는 결정이었음
- ✅ 위 되돌림 반영 — `package.json` version → `0.0.1`(사용자 지정), `MILESTONES.md`는 버전
  경계/포함 범위를 비우고 "Claude가 임의로 정하지 않는다" 원칙을 명시한 빈 틀만 남김. 실제
  마일스톤 내용은 앞으로 사용자와 논의해서 채운다
- ✅ `MILESTONES.md`에 v0.1.0(지도 없이 QR→방화면)/v0.2.0(지도 포함 최소 기능 전체) 스코프와
  지도 화면 처리 방식(막아두기 권장, 삭제 비권장) 기록 — 사용자 확정 내용
- ⚠️ **로드맵 우선순위가 근본적으로 잘못됐었다는 지적** — Staff/Guest Care/좌석배치 에디터/
  운영자 대시보드 등 "손님이 이미 앱을 쓰고 있다"를 전제로 한 부가 기능을 먼저 만들었는데,
  정작 "손님이 매장에서 이 앱을 실제로 켜서 쓸 것인가"라는 핵심 가설이 검증 전이었다. 기능을
  순서대로 다 만드는 로드맵이 아니라 가설 검증/MVP 중심으로 다시 짜야 한다는 지적
- ✅ ADR-0004: 로드맵을 가설 검증/MVP 중심으로 재구성 — `ROADMAP_V2.md`에 "핵심 가설과 MVP
  로드맵" 섹션 신설(핵심 가설: 손님이 매장에서 앱을 실제로 켜서 쓴다 / v0.1.0=지도 없이
  QR→방화면 최소 루프 / Staff·Guest Care 보류 / 기존 Phase 1~10 상세는 "Phase 히스토리"로
  격하하되 삭제하지 않음)
- 🔲 파일럿 매장 찾기 (비개발 트랙, 사용자 담당) — v0.1.0 스코프를 더 좁히려면 선행 필요
- ✅ v0.1.0을 위한 지도 게이팅 — 안 만들기로 결정(ADR-0005 갱신), 해당 없음으로 종료
- ✅ GitHub Actions CI 추가 (`.github/workflows/ci.yml`) — `main`으로의 PR/push마다 tsc·vitest·
  build 자동 실행. required status check로 걸어 실패 시 머지를 막으려면 GitHub 저장소 설정에서
  Branch protection rule을 켜야 하는데, 이건 Claude가 API로 할 수 없어 **사용자가 직접 설정해야
  함** (`AGENTS.md` "CI" 항목에 안내 남김)
- ✅ `AGENTS.md`에 브랜치 이름 규칙 명시 — `feature/`(기능) · `fix/`(버그) · `chore/`(설정/도구) ·
  `docs/`(문서 전용인데 예외적으로 브랜치 필요한 경우)
- ✅ GitHub 저장소 설정에서 `main` 브랜치 Ruleset(`main-protection`) 생성 완료 (사용자 직접 진행)
  — PR 필수(직접 push 금지) + CI(`check`) required status check + force push 차단. Target
  branches를 "Include default branch"로 지정해 `main`에 적용
- ✅ 커밋 메시지 규칙 확정 — `type: 한국어 설명` (`feat`/`fix`/`docs`/`chore`/`refactor`/`test`)
- ✅ 브랜치 이름 설명 부분도 한국어로 통일 (`type/한국어-설명`, 예: `fix/로그인-버튼-안눌림`)
- ✅ "사용자는 실무 워크플로우를 배우는 입장" 원칙을 `AGENTS.md`에 명시 — Claude가 더 표준적인
  실무 관행이 있으면 지시받기 전에 먼저 제안. 계기: `chore/v0.3.0-release-bookkeeping` 브랜치
  이름에 확정 안 된 버전 번호를 박아넣었던 실수를 교훈으로 기록
- ✅ PR #3 머지 완료 (`main` 2cf3a1c) — 버전 0.0.1, MVP 로드맵, CI, 브랜치 보호, 커밋/브랜치
  규칙 전부 반영. 로컬 `main`도 동기화
- ✅ 파일럿 검증 기준 초안 — `ROADMAP_V2.md`에 "검증 기준" 섹션 추가. 숫자(%) 기준은 아직
  안 정하고, 정성적 최소 조건("운영자 유도 없이 손님이 스스로 리액션 1개 이상 사용")만 먼저
  정의. 숫자 기준은 1차 파일럿 데이터를 보고 정하기로 함(경험 없이 임의로 정하지 않기 위함)
- ✅ 서비스 플로우 다이어그램을 저장소에 편입 (`docs/b2b-pivot/diagrams/service-flow.drawio`) —
  ADR-0001(전용 좌석 선택 화면)·ADR-0002(지도 매장 미리보기) 반영해 갱신, 코드 미반영 부분은
  점선 박스로 표시. `AGENTS.md` 문서 지도에 추가
- ✅ PR 템플릿 추가 (`.github/PULL_REQUEST_TEMPLATE.md`) — Summary/Test plan/관련 문서 구조
- ✅ 장애 대응 가이드 추가 (`AGENTS.md`) — Vercel Promote to Production으로 즉시 롤백 → 원인
  수정은 평소 워크플로우대로 → PROGRESS.md에 기록
- ✅ ADR-0005: 3개 가설(A=손님이 앱을 켜서 쓴다 / B=사장님이 AI 운영 리포트를 필요로 한다 /
  C=외부인이 실시간 분위기 데이터를 원한다) 검증 순서 확정 — A 최우선(B·C의 전제조건) → A
  검증 후 C(이미 구현됨, 저비용)를 B(신규 구현, 고비용)보다 먼저 → B는 실제 구현 전에
  인터뷰/가짜 버튼으로 먼저 검증. `ROADMAP_V2.md` "핵심 가설과 MVP 로드맵" 섹션 갱신
- ⚠️ **같은 날 수정**: 사용자가 "지도 게이팅 없이 A/C를 동시에 검증하자"고 재결정 — 지도가
  이미 다 구현돼 있는데 그걸 숨기는 로직을 새로 만드는 게 이 시점엔 불필요한 엔지니어링이라는
  판단. 리스크(A 실패 시 C 결과도 애매해짐)는 A/C 지표를 처음부터 분리 관찰하는 걸로 완화
- ✅ 위 재결정 반영 — ADR-0005(아직 미병합이라 새 ADR 대신 직접 갱신)/`ROADMAP_V2.md`/
  `MILESTONES.md` 전부 수정. v0.1.0 = 지도 게이팅 없이 `main` 전체 범위로 재정의, v0.2.0
  개념은 폐기(v0.1.0에 흡수)
- ✅ PR #4 머지 완료 (`main` d42d29a) — 가설 A/B/C 정리, 검증 기준, 서비스플로우 다이어그램,
  PR템플릿, 장애대응 가이드 전부 반영. 로컬 `main` 동기화
- ✅ TODO 정리 — 지도 게이팅 TODO는 "해당없음"으로 종료, ADR-0001은 v0.1.0 필수로 명확화,
  ADR-0002는 v0.1.0 필수 스코프 아님으로 명확화
- ✅ **ADR-0001 코드 반영 완료** — `/room/[code]/seat` 전용 좌석 선택 화면 신설(자리배치도를
  화면 대부분에 크게 표시, 좌석 선택 = 방 입장 완료). 방 화면은 좌석 미선택 손님을 그리로
  리다이렉트하고, 손님용 자리배치도는 읽기 전용으로 전환. 도달 불가능해진 `VenueTab`의
  "좌석 선택 전" 분기(`hasSeat`/`hotDisabledLabel`) 제거. tsc/테스트(179)/빌드 통과
- ⚠️ **PR #6 테스트 중 발견**: `/dev/rooms`로 테스트 입장해도 손님 화면이 안 보인다는 사용자
  지적 — 방 화면 `isHost`가 참여자 `session_token`이 아니라 운영자 로그인 세션(Supabase
  Auth)으로만 판정돼서, 운영자로 로그인한 브라우저에서는 손님 세션으로 바꿔도 계속 운영자
  화면이 뜨는 버그였다
- ✅ 위 버그 수정(1차) — `/dev/rooms`의 "테스트 입장"이 같은 탭에서 바로 넘어가지 않고, 로그인
  안 된 브라우저에서 열어야 하는 링크(`/dev/rooms/enter?code=...&token=...`)를 발급하는
  방식으로 변경
- ⚠️ **사용자 피드백**: 그마저도 Vercel 배포 보호(로그인 요구) + 시크릿창 왕복까지 겹쳐서
  테스트 흐름이 너무 번거롭다는 지적 — "프리뷰 테스트 플로우 싹 다 갈아엎어야겠다"
- ✅ 위 버그 수정(2차, 최종) — 별도 브라우저 방식을 폐기하고 `/dev/rooms/enter` 삭제,
  대신 `sessionStorage` 기반 미리보기 플래그(`setPreviewAsGuest`/`isPreviewingAsGuest`/
  `clearPreviewAsGuest`, `src/lib/session.ts`)를 도입 — 서버의 실제 `isHost` 판정은 그대로
  두고, 방 화면·좌석 선택 화면의 **클라이언트 렌더링만** `effectiveIsHost`로 뒤집는다.
  `/dev/rooms`가 다시 같은 탭에서 바로 이동하되 이제는 정확히 손님 화면이 뜬다. 두 화면
  모두 "손님 화면 미리보기 중" 배너 + 종료 버튼 추가. tsc/테스트(179)/빌드 통과, PR #6에 포함
- ✅ Vercel Deployment Protection(프리뷰 로그인 요구) 관련 — 근본 해결은 프로젝트 설정에서
  꺼야 함(Claude가 API로 할 수 없음, 사용자 안내 필요). `/dev/rooms` 자체의 번거로움은
  위 수정으로 해소됨

**남은 TODO (다음 세션/대화에서 이어감)**:
- 🔲 파일럿 매장 찾기 (비개발, 사용자)
- 🔲 ADR-0002 코드 반영 — 지도 매장 미리보기 (v0.1.0 필수 아님, 나중으로 미뤄도 됨)

## 2026-08-11

- ✅ **ADR-0006: QR 스캔 필수 규칙 폐지, 지도 "입장하기" 버튼을 기본 입장 경로로** — 가설
  A/C 검증을 더 빠르고 간편하게 하려고 사용자가 결정. 운영자는 매장 등록만 하면 되고(QR
  인쇄/설치 불필요), 손님은 지도에서 매장을 찾아 "입장하기" 버튼만 누르면 된다. 코드
  확인 결과 `/v/[venueId]` 랜딩 화면은 애초에 QR 스캔 여부를 검증하지 않았고, 실제 방문
  검증은 처음부터 위치 반경 체크(`geofence_radius_m`)가 전담해왔다 — 지도 바텀시트의
  "입장하기" 버튼도 이미 같은 URL로 연결되어 있었음(§2.7, Phase 9). 그래서 이 변경은
  보안 수준을 낮추지 않고, **코드 변경 필요분도 최소**(운영자 설정 화면의 "매장 고정
  QR" 카드 취급만 재검토하면 됨)다. `ROADMAP_V2.md` Phase 1.5(완료·병합됨)의 "QR 필수"
  전제에 대한 직접 재확정이라 `AGENTS.md`의 "완료된 Phase 예외" 조항 적용 — ADR과
  ROADMAP_V2.md에 사실을 남기고 진행. `BUSINESS_RULES.md` §1.1/§2.1/§2.2/§2.7,
  `ROADMAP_V2.md`, `MILESTONES.md` v0.1.0 범위 문구 갱신. **이번엔 문서만 반영, 코드는
  미착수**(사용자가 명시적으로 "문서화만 진행" 지시)
- 🔲 ADR-0006 코드 반영 (미착수, 승인 대기) — `src/app/operator/settings/[id]/page.tsx`의
  "매장 고정 QR" 카드를 필수 온보딩에서 뺄지/UI를 어떻게 바꿀지 미확정, §1.1/§2.1/§2.2/§2.7
  문구 정정, 온보딩/홈 화면에 남아있는 "QR 스캔" 안내 문구 정리
- 🔲 **기능 정리(덜어내기) 후보 — 스코프 미확정, 열린 논의 주제로만 기록**: 사용자가 "지금
  서비스는 덜어내는 과정이 필요함"이라며 표현하기/직원 친절도/운영 리포트/운영 메모/참가자
  리스트 등을 예로 들었다. 아직 "무엇을 어떻게(완전 제거/뒤로 미룸/축소) 정리할지"는 결정된
  바 없어 `BUSINESS_RULES.md`/`ROADMAP_V2.md`에는 반영하지 않았다 — 구체적인 범위가 정해지면
  `CHANGE_INTAKE_PROCESS.md` 절차(도메인 매핑 → 로드맵 영향 분석 → 승인 → 코드)를 밟는다

## 2026-08-12

- ✅ **좌석 선택 화면 — 미니맵 + 2단계 선택 흐름 (CGV 좌석 예매 UI 참고)**: 참여자용 좌석 선택
  화면(`/room/[code]/seat`, `BUSINESS_RULES.md` §2.8)에 세 가지를 추가하기로 확정 — (1) 전체
  좌석 배치를 축소해 보여주는 미니맵을 항상 표시, (2) 미니맵 위에 현재 스크롤/뷰 영역을
  사각형으로 표시, (3) 화면 하단에 "선택완료" 버튼 신설. 기존엔 좌석을 탭하는 즉시 방 입장이
  확정됐는데, 이번 확정으로 **탭 = 하이라이트(가선택), "선택완료" 버튼 = 최종 확정**의 2단계
  흐름으로 바뀐다(확정 전 재선택 가능) — 사용자가 두 선택지 중 직접 선택. 새 도메인이 아니라
  Phase 9에서 이미 완료·병합된 화면의 UI 확장이라, 새로운 원본 데이터 없이 기존
  `venue_seats`/`venue_layout_items`를 그대로 재사용한다. `BUSINESS_RULES.md` §2.8 갱신 완료.
  **문서만 반영, 코드 작업은 아직 시작 전** — 별도 세션/브랜치(`feature/`)에서 착수 예정
- 🔲 위 좌석 선택 화면 UI 확장 코드 반영 (미착수, 승인 대기) — 미니맵 컴포넌트, 뷰포트 표시,
  2단계 선택 상태 관리(가선택 vs 확정), "선택완료" 버튼 및 하단 고정 레이아웃
- ✅ **"문서만 고칠 땐 브랜치 불필요" 규칙 조건 명확화** — 사용자가 지적: 이 규칙은 이미
  작업 중인 feature 브랜치가 있을 때만 유효하다(문서 커밋이 그 PR에 자연스럽게 실림). 지금
  세션처럼 작업 중인 feature 브랜치 없이 문서 전용으로 시작하면, 커밋은 세션이 자동 배정한
  `claude/세션ID` 브랜치에 쌓이는데 이것도 결국 `main`엔 PR을 거쳐야 해서 "브랜치 절약"
  효과가 없고 이름 규칙만 깨진 브랜치가 남는다. 이 경우엔 `docs/설명` 브랜치를 새로 만들기로
  확정. `AGENTS.md` "워크플로우" 섹션 갱신 완료
- ✅ PR #8 머지 완료 (`main`) — 위 좌석 선택 화면 미니맵/2단계 선택 흐름(ADR-0007) + 브랜치
  규칙 명확화, 문서만
- ✅ **ADR-0008: 좌석 관리 툴 — 미니맵/확대축소/드래그 전 화면 적용 + 장식 요소 종류 정리 +
  좌석 고정 크기**: `main`에서 새 `docs/좌석관리-툴-미니맵-확대축소` 브랜치를 따서 진행(작업
  중인 feature 브랜치가 없는 상태 → 위에서 확정한 새 규칙 적용). 확정 내용 — (1) 운영자 설정
  좌석 편집 화면("좌석 관리 툴", 비공식 호칭)에 미니맵+뷰포트+확대/축소/드래그(제스처 전용)를
  자체 구현으로 추가, (2) `SeatMap` 컴포넌트를 공유하는 다른 화면(참여자 좌석 선택, 방 화면
  자리배치도)에도 동일하게 적용 — 매장 상세 바텀시트 좌석 탭은 `SeatMap` 미사용이라 제외,
  (3) 장식 요소 `table` 폐지(`box`와 기능 중복)·`line` 신설, (4) 장식 요소 크기 제한(최소
  4%/최대) 전면 폐지 — 매장 규모/다층 구조 대응, (5) 좌석은 개별 크기 조절 없이 전부 동일한
  고정 크기(기존 `table` 평균 크기 근거로 산출, 저장값이 아니라 렌더링 상수라 기존 좌석도
  즉시 반영), (6) 좌석 복제 기능 신설. `BUSINESS_RULES.md` §2.8, `VARIABLES.md` 갱신 완료.
  **문서만 반영, 코드 작업은 아직 시작 전**
- ✅ **ADR-0007 + ADR-0008 코드 반영 완료** (`feature/좌석배치도-미니맵-확대축소`) — 두 ADR이
  같은 화면군의 같은 UI라 한 번에 구현. 신규 `useZoomPan` 훅(핀치/드래그/Ctrl+휠, 버튼 없음)과
  `CanvasMiniMap`을 만들어 `SeatMap`에 내장 → 참여자 좌석 선택·방 화면·매장 상세 바텀시트 3개
  화면에 동시 반영. 운영자 좌석 관리 툴은 ADR대로 캔버스를 공유하지 않고 제스처 계산만 훅으로
  공유. 좌석 선택은 2단계 흐름(탭=가선택 / "선택완료"=확정)으로 전환. `table` 폐지·`line`
  신설(마이그레이션 0017), 크기 제한 제거, 좌석 고정 크기 8%, 좌석 복제 기능 추가.
  tsc/vitest(188)/build 통과, Playwright로 4개 화면 실제 검증. 미확정이었던 4개 항목(좌석 크기
  값·line 좌표 모델·복제 트리거·`table` 행 처리)의 결정 근거는 ADR-0008 "후속 작업"에 기록
- ⚠️ **구현 중 발견한 문서-코드 불일치 2건** (ADR-0008에 정정 기록):
  (1) ADR은 "`SeatMap`에 한 번 구현하면 세 화면에 자동 반영"이라고 했지만, 실제로는 **방 화면의
  운영자 좌석 이동 뷰가 `SeatMap`을 안 쓰는 별도 인라인 캔버스**였다(손님 뷰만 사용). 그대로 두면
  운영자만 확대/미니맵이 없어져서, 운영자 뷰를 `SeatMap`으로 통합하고 운영자 전용 상호작용만
  prop으로 얹었다. (2) 좌석 크기를 px로 두면 확대 시 배경 배치 요소만 커지고 좌석은 그대로
  남아 배치가 어긋난다 — 고정 크기를 px가 아니라 **캔버스 대비 %**로 정의해 해결. 둘 다 확대
  기능을 넣기 전엔 드러나지 않던 문제라 ADR 작성 시점엔 알 수 없었던 것
- ⚠️ **기존 테스트 3건 수정** — `layout/route.test.ts`가 폐지 대상인 규칙(최소 크기 4% 클램프,
  `kind: 'table'` 사용)을 검증하고 있어서, ADR-0008 확정 내용에 맞게 갱신(크기 제한 폐지 검증 +
  `table` 400 / `line` 200 검증 추가). 좌석 복제 라벨 규칙은 `seatLabel.test.ts` 신규 5건
- ✅ **ADR-0008 범위 정정 — 매장 상세 바텀시트 좌석 탭 포함**: 처음엔 이 탭이 `SeatMap`을 안
  쓰는 단순 점유 숫자 카운트(`VenueDetailSheet.tsx` 실제 구현)라 미니맵 적용 대상에서 뺐는데,
  사용자가 "좌석배치도인데 왜 빼냐"고 지적. 확인해보니 §2.7 원래 규칙 문구도 "어떤 좌석이
  비어있는지"였지 집계 숫자가 아니었다 — Phase 9 구현 때 이미 문서 의도보다 단순화됐던 것.
  이 탭을 실제 `SeatMap` 캔버스(읽기전용)로 승격하고, 미니맵+뷰포트+확대/축소/드래그 적용
  대상에 포함하기로 확정. `BUSINESS_RULES.md` §2.7/§2.8, ADR-0008 갱신 완료

## 2026-08-13

- ⚠️ **프리뷰 검토 중 발견 — 데스크톱에서 Tailwind 유틸리티 클래스가 전혀 생성되지 않음
  (이번 브랜치와 무관, `main`에도 존재하는 기존 버그)**: 사용자가 Vercel 프리뷰 스크린샷으로
  "데스크톱에서 좌석 크기가 이상하다"고 보고 → 조사 결과 좌석 크기 계산 로직 자체는 정상(캔버스
  폭 대비 %로 정확히 동작)이었고, 진짜 원인은 `src/app/layout.tsx`의 루트 `max-w-md mx-auto`
  래퍼가 컴파일된 CSS에 아예 존재하지 않는 것이었다(`computedMaxWidth: "none"` 확인, 빌드 결과물
  전체에서 `flex`/`grid`/`px-6` 등 Tailwind 유틸리티 클래스가 전무 — `max-w-md` 하나만의 문제가
  아니라 앱 전체의 빌드 설정 문제). 수정 전 `main`으로 체크아웃해 동일하게 재현되는 것까지 확인해
  이번 좌석 배치 작업과 무관함을 검증. **별도 `fix/` 브랜치에서 처리하기로 사용자와 합의**,
  이번 브랜치엔 포함하지 않음
- ✅ **좌석 복사 버튼 라벨 정정 + 네모 기본 텍스트 제거**: 사용자 요청 두 가지 — (1) 복제 버튼
  라벨을 "복제"→"복사하기"로 통일(기능은 기존 그대로), (2) 네모(box) 요소 추가 시 강제로 붙던
  기본 텍스트 `'주방'`을 제거하고 빈 값 + 입력 안내 placeholder("표시할 이름 (비워도 됨)")로
  대체 — 운영자가 실제로 뭘 표시할지 직접 입력하게
- ✅ **"복사하기" 버튼을 배치 요소(네모/출입문/텍스트/선)에도 확대**: 프리뷰 테스트 중 사용자가
  "예약석"이라는 이름의 네모를 복사하려는데 버튼이 안 보인다고 보고 — 원인은 복제 기능이
  `selection.type === 'seat'`일 때만 노출되도록 좌석 전용으로 구현돼 있었기 때문(ADR-0008 최초
  구현 시점엔 배치 요소 복제 자체가 스코프 밖이었음). 운영자가 구역/좌석을 `box`로 표현하는
  게 자연스러운 이상 좌석에만 한정할 이유가 없다고 판단해, `handleDuplicateSeat`를
  `handleDuplicate`로 일반화하고 버튼을 선택 종류와 무관하게 항상 노출하도록 확장. 라벨이 있는
  요소는 좌석과 동일한 끝자리 숫자 증가 규칙을 적용하고, 라벨이 없는 요소(빈 이름 네모/선)는
  번호를 붙이지 않고 그대로 복사. ADR-0008 "후속 작업"에 결정 근거 추가 기록.
  tsc/vitest(188)/Playwright(좌석·네모 양쪽 복사 버튼 노출 및 라벨 증가) 검증 완료
- ✅ PR #11 머지 완료 (`main`) — 자리배치도 미니맵/확대·이동, 좌석 2단계 선택, 복사하기 버튼
  (좌석+배치 요소) 등 ADR-0007/0008 코드 반영 전체
- ✅ **데스크톱 Tailwind 유틸리티 클래스 미생성 버그 — 원인 확정 및 수정**
  (`fix/데스크톱-좌석-크기-표시`): 위에서 발견한 버그의 근본 원인은 저장소에 `postcss.config`
  파일이 아예 없던 것이었다. Tailwind v4 + Next.js 공식 설정은 `@import "tailwindcss";`(이미
  `globals.css`에 있음)만으로는 부족하고, `@tailwindcss/postcss`를 PostCSS 플러그인으로
  등록하는 `postcss.config.mjs`가 반드시 있어야 하는데 이 파일이 처음부터 없어서 Tailwind가
  빌드 파이프라인에 전혀 연결돼 있지 않았다 — 그래서 `@theme inline{...}` 블록조차 처리되지
  않고 컴파일된 CSS에 문자 그대로 남아있었다(원래 3.4KB, 유틸리티 클래스 0개). `postcss.config.mjs`
  추가 후 빌드 결과 CSS가 13.3KB로 늘고 `.max-w-md`/`.flex`/`.px-6` 등 실제 유틸리티 규칙이
  생성됨을 확인. Playwright로 데스크톱(1440px) 뷰포트에서 `.max-w-md` 요소의
  `computedMaxWidth`가 `"none"`→`"448px"`로 정상화되고, 운영자 좌석 관리 툴 스크린샷에서
  좌석·배치요소 비율이 모바일과 동일하게 보이는 것까지 확인. 앱 전체에 영향을 준 설정
  누락이었던 만큼 이 브랜치 전용 회귀는 없었는지 tsc/vitest(188) 통과 확인
- ⚠️ **위 수정 직후 새 증상 발견 — 데스크톱 화면 전체가 가운데가 아니라 왼쪽에 붙음**:
  postcss 설정을 추가해 Tailwind 유틸리티가 실제로 생성되기 시작하자, 이번엔 `layout.tsx`의
  `max-w-md mx-auto` 래퍼가 폭은 448px로 맞는데 `mx-auto`(가운데 정렬)이 전혀 먹지 않는 새
  증상이 드러났다(운영자 매장 설정 화면 스크린샷으로 사용자가 재보고). Playwright로
  `getComputedStyle` 확인 결과 `.mx-auto{margin-inline:auto}` 규칙 자체는 컴파일된 CSS에
  존재하는데도 실제 적용된 `marginLeft`/`marginRight`가 둘 다 `0px`였다.
  **근본 원인**: `globals.css`의 `* { margin:0; padding:0; ... }` 초기화 규칙이 어떤
  `@layer`에도 속하지 않은 "레이어 없는(unlayered)" CSS인 반면, Tailwind의 모든 유틸리티는
  `@layer utilities` 안에 있다 — CSS Cascade Layers 스펙상 레이어 없는 선언은 명시도나
  작성 순서와 무관하게 항상 레이어 안 선언을 이긴다. 그래서 `mx-auto`뿐 아니라 `px-6`을
  포함한 **margin/padding 계열 Tailwind 유틸리티 전부가 이 초기화 규칙에 조용히 무력화되고
  있었다**(앱 전체에서 이런 클래스를 쓰는 파일 14개 확인) — postcss.config가 없어 Tailwind가
  아예 안 돌던 동안은 유틸리티 자체가 없어 드러나지 않았을 뿐. 수정: 이 초기화 규칙을
  `@layer base { ... }`로 감싸 Tailwind의 레이어 순서(`base` < `utilities`)에 정식 편입시킴 —
  Tailwind 자체 preflight도 동일한 margin/padding 리셋을 이미 `@layer base`에 두고 있어
  결과적으로 관용적인 형태로 정리됨. Playwright로 1440px 데스크톱에서 `marginLeft`/
  `marginRight`가 `0px`→`496px`(정중앙)로 정상화됨을 확인, tsc/vitest(188) 재통과 확인
- ⚠️ **발견 — 매장 등록해도 지도에 안 뜬다**: 사용자가 "사장님 매장 등록 시 지도에 매장이
  표시되게 구현해"라고 요청 — 조사해보니 실제로 빠진 기능이었다. `BUSINESS_RULES.md`
  §2.6에 이미 "위경도가 등록된 Venue만 지도 대상"이라고 명시돼 있고 `get_live_hot_venues`
  RPC도 `latitude`/`longitude IS NOT NULL`을 요구하는데, 정작 그 값을 채울 방법이 어디에도
  없었다 — 매장 등록 API(`POST /api/venues`)는 `name`/`category`/`address`만 저장하고,
  운영자 설정 화면에도 위경도 입력 UI가 없었다(`PATCH /api/venues/[id]`는 `latitude`/
  `longitude`를 이미 화이트리스트에 포함하고 있었지만 아무도 호출하지 않음). 즉 위치 등록은
  문서에는 "선택 사항"으로 남아있지만 실제로는 등록할 방법 자체가 없었던 것
- ✅ **위 문제 해결 — 주소 자동 지오코딩으로 위경도 채움** (`feature/매장-지도-위치-자동등록`):
  구현 방식은 세 가지(현재 위치 버튼 / 주소 자동 변환 / 둘 다) 중 사용자가 "주소 자동
  변환(지오코딩)"을 선택. 카카오맵 JS SDK가 이미 `libraries=services`로 로드돼 있어 별도
  REST API 키 없이 `kakao.maps.services.Geocoder`로 처리 가능. `src/hooks/useKakaoMap.ts`에
  `geocodeAddress(address)` 함수 신설, 운영자 설정 화면(`/operator/settings/[id]`)의
  `handleSave`에서 주소가 있으면 저장 직전 지오코딩해 `latitude`/`longitude`를 함께
  PATCH — 주소를 지우면 위치도 같이 비우고, 지오코딩 실패(주소가 애매한 경우 등) 시엔
  기존 위치를 그대로 둔 채 경고만 보여주고 나머지 설정 저장은 막지 않는다. tsc/vitest(188)
  통과, Playwright로 카카오 SDK를 모킹해 성공/실패 두 경로 모두 확인(성공 시 PATCH 페이로드에
  위경도 포함, 실패 시 경고 노출+나머지 필드 정상 저장)
